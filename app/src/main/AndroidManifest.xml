<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lizarda.lizarda">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.detail_produk.DetailProdukActivity"
            android:label="@string/title_activity_detail_produk"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.Sign.SignupActivity" />
        <activity android:name=".ui.Sign.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.Sign.ResetPasswordActivity" />
        <activity android:name=".ui.profile.ProfileActivity" />
        <activity
            android:name=".ui.MainActivity"
            android:label="Main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.list_produk.ProductListActivity"
            android:label="@string/title_activity_detail_category_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.add_product.AddProductActivity"
            android:label="@string/title_activity_add_product"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.profile.edtProfileActivity"></activity>
    </application>

</manifest>